# Three.js 0.185.1

The two ESM builds and MIT license are copied without modification from the
published `three@0.185.1` npm archive:
https://registry.npmjs.org/three/-/three-0.185.1.tgz

Used by the p-Brain puzzle block only. Both files are local so the presentation
does not need a CDN or an internet connection.
